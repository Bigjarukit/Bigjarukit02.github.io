import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/Bigjarukit02.github.io",
  assetPrefix: "/Bigjarukit02.github.io/",

  // basePath: '/next-project',
  /* config options here */
};

export default nextConfig;
